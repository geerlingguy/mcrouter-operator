kind: Service
apiVersion: v1
metadata:
  name: '{{ meta.name }}-memcached'
  namespace: '{{ meta.namespace }}'
spec:
  ports:
  - port: {{ memcached_port }}
    protocol: TCP
  selector:
    matchLabels:
    app: '{{ meta.name }}-cache'
